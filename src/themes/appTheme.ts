import { createTheme } from '@mui/material'

const appTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#f17e0b',
    },
  },
  typography: {
    fontFamily: 'Lato',
  },
})

export default appTheme
